/**
 * Синхронизирует состояние темы с тем, что уже применил критический
 * инлайн-скрипт из nuxt.config (он ставит класс .light до первой
 * отрисовки, чтобы не было вспышки).
 *
 * Без этого вызова isDark оставался бы в начальном значении true
 * независимо от реальной темы: иконка переключателя показывала бы не ту
 * сторону, а первый клик по нему не давал бы видимого эффекта.
 */
export default defineNuxtPlugin(() => {
  useTheme().init()
})
