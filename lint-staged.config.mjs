export default {
  // Форматирования здесь нет намеренно: выравнивание значений по колонкам
  // в этом проекте расставлено руками, и prettier его бы схлопнул.
  '*.{ts,mjs,vue}': [
    'eslint --fix',
    // Только спеки, связанные с изменёнными файлами: полный прогон ждёт push.
    'vitest related --run --passWithNoTests',
  ],
}
